server:
  port : 8080
spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/citience'
    username: 'citience'
    password: 'citience'
    driverClassName: 'org.postgresql.Driver'
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: 'org.hibernate.dialect.PostgreSQLDialect'

network:
  timeout: 60000